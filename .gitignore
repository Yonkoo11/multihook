
# App build artifacts (vite dev cache); production output goes to /docs which IS committed for GH Pages
# Don't commit deploy keypairs (or do, depending on your devnet vs prod setup; for now keep out)
# Local env files (Helius API key, etc.) — NEVER commit
**/*.rs.bk
.DS_Store
.anchor
.env
.env.local
.yarn
/tmp/multihook-*
app/.dev-key.json
app/.env
app/.env.local
app/dist
app/public/.dev-key.json
app/tsconfig.tsbuildinfo
node_modules
scripts/beats.json
target
target/deploy/*-keypair.json
test-ledger
video/judge-frames/
video/raw-recording-v4.mp4
